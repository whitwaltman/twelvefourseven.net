---
layout: layouts/base.njk
---
<p>
    This is a <strong>now</strong> page. It's a way for me to share what I'm focused on
    at this point in my life. My focus can often shift, so it's helpful
    for me to keep track of what I'm doing and thinking about.
</p>
<p>
    If you have your own site, you should consider {% a "https://nownownow.com/about", "adding a now page" %}.
</p>

{% for i in range(0, now.length) %}
<div {% if i > 0 %} class="update then"
    {% else %} class="update" {% endif %}>
    <div class="status-bar">
        {% if i == 0 %}
        <span>NOW</span>
        <span class="date text-neutral-400">Updated {{ now[i].date | simpleDate }}</span>
        {% else %}
        <span>THEN</span>
        <span class="date text-neutral-500">{{ now[i].date | simpleDate }}</span>
        {% endif %}
    </div>
    <ul>
        {% for bullet in now[i].content %}
        <li>{{ bullet }}</li>
        {% endfor %}
    </ul>
</div>
{% endfor %}