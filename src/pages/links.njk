---
title: links
description: A bunch of links I like. Explore the vast, independent web!
section: links
---
{% for link in links %}
    <div class="mb-4">
        <div class="flex flex-col md:flex-row justify-between">
            <div><a class="ext-link" target="_blank" rel="noopener noreferrer"
                href="{{ link.url }}">{{ link.title }}</a></div>
            <span class="text-sm text-gray-400">{{ link.created | convertTimestamp }}</span>
        </div>
    </div>
{% endfor %}