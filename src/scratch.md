---
date: '2026-02-08T12:00:00Z'
preview: Useful commands
---

`ssh vps` -> twelvefourseven.net

`ls -1 | grep "^[a-z]"` -> only lowercase directories

`cd ~/org && ls -1 | grep -v "~"` -> exclude org backup files
