---
title: links
date: Last Modified
section: links
---
<p>
Links are the lifeblood of the web, and linking to other sites is a declaration of independence. —<a href="https://thoughts.melonking.net/thoughts/every-site-needs-a-links-page-why-linking-matters">
    Every site needs a Links Page / Why linking matters
</a></p>
{% for link in links %}
    <div class="mb-4">
        <div class="flex flex-col md:flex-row justify-between">
            <div><a class="ext-link" target="_blank" rel="noopener noreferrer"
                href="{{ link.url }}">{{ link.title }}</a></div>
            <span class="text-sm text-gray-400">{{ link.created | convertTimestamp }}</span>
        </div>
    </div>
{% endfor %}